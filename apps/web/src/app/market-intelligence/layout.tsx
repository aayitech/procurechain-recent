export const metadata = {
  title: 'Market Intelligence Centre',
  description: 'Procurement market intelligence: what changed, why it matters, and what to watch.',
};

export default function MarketIntelligenceLayout({ children }: { children: React.ReactNode }) {
  return children;
}
